# compiled binaries
emily-agent/emily-agent
cmd/get-gmail-token/get-gmail-token

# runtime state dirs
emily-agent/conversations/
emily-agent/emily-state/
emily-agent/emily-memory/
emily-agent/fartco-memory/
emily-collect/

# construct artifacts
EMILY_CONSTRUCT.txt

# integration runtime state
signals/observations/.last-processed
signals/tasks/.last-processed

# Go build cache
*.test

# Go workspace (local only)
go.work
go.work.sum

# Secrets and credentials — never committed
var/emily-secrets.env
var/*.env
*-secrets.env
*.env.local
var/pexels.txt
var/cloudflare.md
var/twillio

# Runtime state subdirs
context/var/
emily-agent/var/
var/logs/

# emily start PID files (rewritten every restart, not meaningful history)
var/*.pid
