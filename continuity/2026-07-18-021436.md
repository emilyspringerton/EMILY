# Continuity Report — 2026-07-18 02:14:36 UTC

## 1. Full sync sweep

All repos clean vs. origin at report time. Known, deliberate exceptions: EMILY's own
auto-regenerated cycle-state files (`context/full-system-context.md`, `context/golden-cache.json`,
`emily-memory/cycle-log.md`, `var/backlog-curated.txt`) and one pre-existing, not-mine, untouched
migration diff in IDUNA (`202606180001_local_users.sql`, still needs its own decision later).

New repo this stretch: `shankpit-460` (forked from `SHANKPIT` tag `460`).

## 2. Changelog digest (since last continuity report, 2026-07-18 00:14:06)

This was an eventful stretch — a real incident, several real fixes, and a new project fork, all
in one continuous session.

**okemily.com (OKEMILY repo):**
- HTTPS/HSTS confirmed live (certbot ran successfully).
- Real blog live with 6 posts (Emily Way reflection, IAM-as-moat, small-teams/security framing,
  Stillness-outline piece, repo spotlight, full-session retrospective, succession-watch closer —
  7 total by report time), all guest-authored by Claude Code, all grounded in real session events.
- API playground (Swagger UI) live, pointed at IDUNA's real OpenAPI spec (fixed a bug: the spec's
  `servers[]` was localhost-only).
- Real ecosystem status page live (`status.html`), backed by `IDUNA/internal/statuspage` —
  real uptime history from day one, includes a UDP-port-liveness check type added specifically for
  the new SHANKPIT-460 game server.
- **Incident**: a `sudo cp` deploy overwrote the live, certbot-managed nginx config with a stale
  repo copy, taking HTTPS down for real for a few minutes. Root-caused, fixed (re-ran certbot),
  and the repo config resynced + a hard rule added to `CLAUDE.md` so it can't repeat quietly.
- `newssite` moved to its own `news.okemily.com` subdomain — fixes ticker sub-pages that 404'd
  under a `/news/` subpath proxy (newssite has no base-path config option).

**IDUNA:** new `internal/blog` (static-HTML blog backend, no PHP/MySQL — deliberate given the box's
memory state), new `internal/statuspage` (real health-check + uptime-history backend, UDP-port
check type added), OpenAPI spec server-URL fix. Mailchimp integration live-verified end-to-end
(one real "subscribed" contact proves the double opt-in path genuinely works).

**PRRJECT_FATBABY:**
- `newssite` given systemd supervision (found and fixed a broken, stale drafted unit that
  referenced a nonexistent flag — would have failed to start outright).
- New NORTHSTAR: `docs/headlines/live-feed-northstar.md` — MTWire-style combined live headline
  feed, builds on the existing (not-yet-running) `feedserver` TCP infrastructure and the existing
  `pr-feed.md` draft. Design only, phased build plan, golden-indexed.
- **Real, live-caught architectural finding**: `entity-graph`/`signalapi`/`newssite` all replay
  their *entire* event-store history into memory on every process start, with no persisted/cached
  index. Migrating `signalapi` to systemd tonight triggered exactly this rebuild, which thrashed
  memory badly enough to be stopped and disabled rather than risk a cascade; `newssite` (already
  supervised) kept OOM-killing on the same pattern independently. Visible symptom: ticker pages
  intermittently claimed "we don't cover AMZN" despite 1,225 real events existing — an indexing
  race, not a coverage gap. This is now BACKLOG S153-14, explicitly flagged as needing real design
  (bounded replay window, or a persisted/cached index — the founder's own "cache it in Mongo or
  something" instinct, aimed at the right target this time), not something improvised live.
- `ops/env.production` gitignore gap fixed earlier this session; a genuinely stray-Claude-session
  memory hog was killed mid-stretch, freeing real memory (~300MB→~800MB free at the time).

**shankpit-460 (new repo):** forked from `SHANKPIT` at tag `460` (commit `55b80f7`). Mission:
stripped-down, low-system-spec competitive esports FPS for a large global audience — explicitly
NOT carrying forward SHANKPIT's persistent-world/DragonsNShit ambitions. Game server (raw UDP
`:6969`) built, running, and systemd-supervised; live-verified auto-restart via a SIGKILL test.
Design/stripping plan (what actually gets cut) is still open — deliberately not improvised.

## 3. Continuity state

**In-flight / needs founder attention:**
- `signalapi` is **stopped and disabled** (not just down — intentionally off) pending a real fix
  for the full-replay memory fragility (S153-14). It will not come back on its own.
- `entity-graph`'s systemd migration was **not done** — still the original unsupervised `go run`
  process from Jul 16, deliberately left alone given what just happened with `signalapi`.
- IDUNA's uncommitted `202606180001_local_users.sql` diff (pre-existing, not this session's) still
  needs a decision.

**Blocked / founder-action-required:**
- S153-06: board of directors / non-profit ownership structure — explicitly parked, no urgency.
- S153-09: OpenAPI spec reconciliation (missing blog/mailing-list endpoints, stale duplicate
  `openapi.yaml`) — deferred by explicit instruction.
- S153-14: entity-graph/signalapi/newssite replay-fragility — needs real design work, not a
  tonight fix.
- Kubernetes/k3s migration northstar — still owed from earlier in the session, still not written.

**System health (verified, not assumed) at report time:**
- `iduna.service`, `emily-system.service`: active, stable.
- `secwatch`/`processor`/`prwatch`/`prwatch-body`/`eps-reconciler`: active, stable (the earlier
  crash-loop, traced to a stray Claude session + this same replay-fragility class of issue, has
  not recurred since that session was killed).
- `fatbaby-newssite.service`: active, but has OOM-killed and auto-restarted multiple times this
  stretch (02:04, 02:11) — systemd's auto-restart is working as designed, but the underlying cause
  (S153-14) is not fixed, just contained.
- `fatbaby-signalapi.service`: intentionally stopped/disabled.
- `shankpit460-server.service`: active, stable, auto-restart verified live.
- System memory: fluctuating significantly through this stretch (as low as ~280MB free during the
  signalapi thrash, recovered to ~860MB free after stopping it) — still the dominant constraint on
  everything done tonight.

## 4. Clear next steps

1. Decide S153-14's real fix (bounded replay window vs. persisted/cached index) — this is the
   single highest-value remaining item; it's already caused two near-incidents in one night.
2. Re-enable `signalapi` once S153-14 has an actual fix, not before.
3. Consider whether `entity-graph` needs the same systemd treatment now, given the demonstrated
   risk, or whether it's safer left alone until S153-14 lands.
4. Founder: decide on `shankpit-460`'s actual design/stripping plan (a real NORTHSTAR.md for that
   repo) before writing more game code there.
5. Kubernetes/k3s northstar is still owed — worth doing before the Mercury-gated infra window
   opens (2026-07-23 onward).
6. IDUNA's stray `202606180001_local_users.sql` diff needs a decision (commit as a proper new
   migration, or discard).
